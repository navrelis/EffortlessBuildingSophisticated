# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Shared mod metadata and the Minecraft version: ../gradle/shared.properties

# https://projects.neoforged.net/neoforged/neoforge
neo_version=21.1.217
# The loader version range can only use the major version of FML as bounds.
neoforge_loader_version_range=[1,)
# https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Sophisticated Core/Backpacks (official NeoForge builds, Modrinth maven).
sophisticatedcore_version=1.21.1-1.5.1.2341
sophisticatedbackpacks_version=1.21.1-3.26.3.2158
# Dev runs only: the builds above need NeoForge >= 21.1.229, so runClient/runServer use the newest builds that
# still run on neo_version (21.1.0+). BackpackScanCompat handles their void runOnBackpacks.
sophisticatedcore_dev_runtime_version=1.21.1-1.4.86.2259
sophisticatedbackpacks_dev_runtime_version=1.21.1-3.25.77.2086
# Curios API, compile only (fallback scan of worn backpacks, see CuriosCompatHelper).
curios_version=9.5.1+1.21.1
