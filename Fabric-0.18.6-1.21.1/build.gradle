plugins {
    id 'fabric-loom' version '1.10.1'
    id 'maven-publish'
    id 'java-library'
    id 'idea'
}

tasks.named('wrapper', Wrapper).configure {
    // Define wrapper values here so as to not have to always do so when updating gradlew.properties.
    // Switching this to Wrapper.DistributionType.ALL will download the full gradle sources that comes with
    // documentation attached on cursor hover of gradle classes and methods. However, this comes with increased
    // file size for Gradle. If you do switch this to ALL, run the Gradle wrapper task twice afterwards.
    // (Verify by checking gradle/wrapper/gradle-wrapper.properties to see if distributionUrl now points to `-all`)
    distributionType = Wrapper.DistributionType.BIN
}

version = mod_version
group = mod_group_id

repositories {
    maven {
        name = 'Fabric'
        url = 'https://maven.fabricmc.net/'
    }
    mavenCentral()

    maven {
        // Registrate and Flywheel maven
        name = 'tterrag maven'
        url = 'https://maven.tterrag.com'
    }
    maven {
        // Ponder, Flywheel, Catnip
        url = "https://maven.createmod.net"
    }
    maven {
        // CurseMaven for optional Sophisticated Backpacks/Storage coordinates
        url = "https://www.cursemaven.com"
    }
    maven { url = "https://raw.githubusercontent.com/Fuzss/modresources/main/maven" } // Config API port needed by Ponder
    mavenLocal()
}

base {
    archivesName = "${mod_id}-fabric"
}

java {
    // Mojang ships Java 21 to end users in 1.21.1, so mods should target Java 21.
    toolchain.languageVersion = JavaLanguageVersion.of(21)
    withSourcesJar()
}

loom {
    runs {
        client {
            client()
        }
        server {
            server()
            vmArgs '-Dfabric.log.level=debug'
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

// Sets up a dependency configuration called 'localRuntime'.
// This configuration should be used instead of 'runtimeOnly' to declare
// a dependency that will be present for runtime testing but that is
// "optional", meaning it will not be pulled by dependents of this mod.
configurations {
    runtimeClasspath.extendsFrom localRuntime
}

configurations.configureEach {
    // flywheel-fabric already bundles these classes; remapping both jars causes duplicate-class failures.
    exclude group: "dev.engine-room.flywheel", module: "flywheel-fabric-api-${flywheel_minecraft_version}"
}

def useCurseRuntimeDeps = (findProperty("useCurseRuntimeDeps") ?: "false").toBoolean()
def embedCreateDeps = (findProperty("embedCreateDeps") ?: "true").toBoolean()
def useLocalRuntimeMods = (findProperty("useLocalRuntimeMods") ?: "false").toBoolean()
def otherModsTree = fileTree("other_mods") {
    include "*.jar"
    exclude "ForgeConfigAPIPort-*.jar"
    exclude "forgeconfigapiport-*.jar"
    exclude "flywheel-*.jar"
    exclude "ponder-*.jar"
}
def fallbackCompileOnlyModsTree = fileTree("../Neoforge-21.1.217-1.21.1/other_mods") {
    include "sophisticatedcore-*.jar"
    include "sophisticatedbackpacks-*.jar"
    include "curios-*.jar"
}
def remappedPortingTransferTree = fileTree(".gradle/loom-cache/remapped_mods") {
    include "**/core-*.jar"
    include "**/transfer-*.jar"
}
def compileSupportModsTree = !otherModsTree.isEmpty() ? otherModsTree : fallbackCompileOnlyModsTree

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"
    mappings loom.officialMojangMappings()
    modImplementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${fabric_api_version}"

    if (embedCreateDeps) {
        // Match NeoForge behavior by shipping required Create rendering stack with the mod jar.
        modImplementation(include("dev.engine-room.flywheel:flywheel-fabric-${flywheel_minecraft_version}:${flywheel_version}")) {
            transitive = false
        }
        modImplementation(include("net.createmod.ponder:ponder-fabric:${ponder_version}+mc${minecraft_version}"))
    } else {
        modCompileOnly("dev.engine-room.flywheel:flywheel-fabric-${flywheel_minecraft_version}:${flywheel_version}") {
            transitive = false
        }
        modCompileOnly "net.createmod.ponder:ponder-fabric:${ponder_version}+mc${minecraft_version}"
        modLocalRuntime("dev.engine-room.flywheel:flywheel-fabric-${flywheel_minecraft_version}:${flywheel_version}") {
            transitive = false
        }
        modLocalRuntime "net.createmod.ponder:ponder-fabric:${ponder_version}+mc${minecraft_version}"
    }
    modLocalRuntime "teamreborn:energy:4.1.0"

    // Optional Sophisticated dependencies from CurseMaven.
    // By default they are compile-only to avoid duplicate runtime mods when using other_mods/*.jar.
    if (useCurseRuntimeDeps) {
        modImplementation "curse.maven:sophisticated-backpacks-unofficial-fabric-port-979322:7147929"
        modImplementation "curse.maven:sophisticated-storage-unofficial-fabric-port-979326:7344673"
    } else {
        modCompileOnly "curse.maven:sophisticated-backpacks-unofficial-fabric-port-979322:7147929"
        modCompileOnly "curse.maven:sophisticated-storage-unofficial-fabric-port-979326:7344673"
    }

    // Default dev setup: prefer Fabric jars in other_mods/. If absent, compile against
    // sibling NeoForge Sophisticated APIs to keep local builds working.
    if (!compileSupportModsTree.isEmpty()) {
        compileOnly(compileSupportModsTree)
    }
    if (!remappedPortingTransferTree.isEmpty()) {
        compileOnly(remappedPortingTransferTree)
    }
    if (useLocalRuntimeMods && !otherModsTree.isEmpty()) {
        modLocalRuntime(otherModsTree)
    }

    compileOnly "javax.annotation:javax.annotation-api:1.3.2"
    compileOnly "com.google.code.findbugs:jsr305:3.0.2"

    // TODO: replace with Fabric Curios equivalent if needed (e.g., Trinkets).
}

processResources {
    inputs.property "version", project.version
    inputs.property "minecraft_version", project.minecraft_version
    inputs.property "fabric_loader_version", project.fabric_loader_version
    filteringCharset = 'UTF-8'

    filesMatching("fabric.mod.json") {
        expand(
                version: project.version,
                minecraft_version: project.minecraft_version,
                fabric_loader_version: project.fabric_loader_version,
                mod_id: project.mod_id,
                mod_name: project.mod_name,
                mod_description: project.mod_description,
                mod_authors: project.mod_authors,
                mod_license: project.mod_license
        )
    }
}

// Example configuration to allow publishing using the maven-publish plugin
publishing {
    publications {
        register('mavenJava', MavenPublication) {
            from components.java
        }
    }
    repositories {
        maven {
            url "file://${project.projectDir}/repo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8' // Use the UTF-8 charset for Java compilation
    options.release = 21
}

// IDEA no longer automatically downloads sources/javadoc jars for dependencies, so we need to explicitly enable the behavior.
idea {
    module {
        downloadSources = true
        downloadJavadoc = true
    }
}

tasks.jar {
    from('TEMPLATE_LICENSE.txt') {
        rename { 'LICENSE_sophisticatedbuilding.txt' }
    }
}
