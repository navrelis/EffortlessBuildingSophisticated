package sophisticated.building.config;

/**
 * The three config files of the mod. Fabric: {@code config/sophisticatedbuilding-<type>.json};
 * NeoForge: the COMMON, SERVER (per world) and CLIENT {@code ModConfigSpec}s.
 */
public enum ConfigType {
    COMMON,
    SERVER,
    CLIENT
}
