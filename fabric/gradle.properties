# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# Loom does not support the configuration cache.
org.gradle.configuration-cache=false

# Shared mod metadata and the Minecraft version: ../gradle/shared.properties

# Minecraft versions declared in fabric.mod.json: 26.1 and 26.1.1 are vanilla-identical to 26.1.2 for this mod and
# were run with the release jar (TESTING.md, "Minecraft 26.1 and 26.1.1 check").
fabric_minecraft_version_range=>=26.1 <=26.1.2

# Fabric toolchain
fabric_loader_version=0.19.5
# Minimum fabricloader declared in fabric.mod.json's "depends". This is the oldest version this code was
# run on, not an assumption about the lowest that would work.
fabric_loader_version_min=0.19.5
fabric_api_version=0.155.3+26.1.2
# Minimum Fabric API declared in fabric.mod.json's "depends": the version this code compiles and runs against
# (fabric_api_version), not an assumption about the lowest that would work.
fabric_api_version_min=0.155.3

# Sophisticated Backpacks/Core have no Fabric build for MC 26.1.2, so this build has no backpack integration and no
# Sophisticated dependencies (the Building Upgrades are placeholder items, see FabricBootstrap).
ci_gradle_jdk=25
# Common Protection API (Patbox, maven.nucleoid.xyz): 2.0.0 takes the NameAndId of Minecraft 1.21.9+ and is compiled
# for Java 25 against the unobfuscated 26.x names (1.0.0, with GameProfile, is for the obfuscated versions)
common_protection_api_version=2.0.0
